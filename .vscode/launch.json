{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "cwd": "${workspaceFolder}",
            "command": "npm start",
            "type": "node-terminal",
            "request": "launch"
        },
        {
            "name": "Launch (Bun)",
            "cwd": "${workspaceFolder}",
            "command": "bun dev",
            "type": "node-terminal",
            "request": "launch"
        },
        {
            "name": "Build",
            "cwd": "${workspaceFolder}",
            "command": "npm run build",
            "type": "node-terminal",
            "request": "launch"
        }
    ]
}
